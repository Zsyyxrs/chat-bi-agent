# Virtual Environment
venv/
env/
ENV/
.venv

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# IDE / OS metadata
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Environment variables
.env
.env.local
.env.*.local

# Secret material (defensive — never commit keys/certs/service accounts)
*.pem
*.key
*.p12
*.pfx
credentials*.json
service_account*.json

# Local runtime config (example template is tracked)
config/local.yaml

# Streamlit secrets (streamlit_app/ uses st.secrets)
.streamlit/secrets.toml

# Project-specific
.claude/
.superpowers/
result/
results/
!results/README.md
!results/baseline_*.json
logs/
*.log
*.sqlite
*.db
# Merge / patch residue
*.orig
*.rej

# Testing / type-checking / notebooks
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.ipynb_checkpoints/
.coverage
.coverage.*
coverage.xml
*.cover
htmlcov/
.tox/
.nox/
.dmypy.json
dmypy.json

# Design / planning docs stay local，但架构设计随仓库交付
# （README 的「文档导航」指向它；此前 docs/ 整个被 ignore，导致那个链接对任何
#  clone 仓库的人都是死链）
docs/*
!docs/金融data agent架构设计.md

# External benchmark data (BIRD dev raw files — 340MB+, license restricts redistribution)
benchmarks/bird/

# Example pool (built by scripts/bootstrap_example_pool.py; embeddings blob, ~4MB per 1k rows)
data/example_pool*.jsonl
